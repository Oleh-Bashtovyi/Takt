import { Component } from '@angular/core';

@Component({
  selector: 'app-register',
  template: `<p class="p-12 text-center text-gray-500">Create account — next iteration</p>`,
})
export class Register {}
